<mvc:View
	controllerName="cl.everis.cgr.actvinst.allCGRActvInstAll.controller.Master"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:u="sap.ui.unified"
	xmlns:semantic="sap.m.semantic">

	<semantic:MasterPage
		id="page"
		title="{masterView>/title}"
		navButtonPress="onNavBack"
		showNavButton="true">
		<semantic:subHeader>
			<Bar id="headerBar">
				<contentMiddle>
					<SearchField
						id="searchField"
						showRefreshButton="{= !${device>/support/touch} }"
						tooltip="{i18n>masterSearchTooltip}"
						width="100%"
						search="onSearch">
					</SearchField>
				</contentMiddle>
			</Bar>
		</semantic:subHeader>

		<semantic:content>
<!--			<PullToRefresh
				id="pullToRefresh"
				visible="{device>/support/touch}"
				refresh="onRefresh" />-->
			<!-- For client side filtering add this to the items attribute:  parameters: {operationMode: 'Client'}}" -->
<!--			<List
				id="list"
				items="{
					path: '/Customers',
					sorter: {
						path: 'CompanyName',
						descending: false
						},
					groupHeaderFactory: '.createGroupHeader'
				}"
				busyIndicatorDelay="{masterView>/delay}"
				noDataText="{masterView>/noDataText}"
				mode="{= ${device>/system/phone} ? 'None' : 'SingleSelectMaster'}"
				growing="true"
				growingScrollToLoad="true"
				updateFinished="onUpdateFinished"
				selectionChange="onSelectionChange">
				<infoToolbar>
					<Toolbar
						active="true"
						id="filterBar"
						visible="{masterView>/isFilterBarVisible}"
						press="onOpenViewSettings">
						<Title
							id="filterBarLabel"
							text="{masterView>/filterBarLabel}" />
					</Toolbar>
				</infoToolbar>
				<items>
					<ObjectListItem
						type="{= ${device>/system/phone} ? 'Active' : 'Inactive'}"
						press="onSelectionChange"
						title="{CompanyName}"
						numberUnit="{Country}">
					</ObjectListItem>
				</items>
			</List>-->
	 	<Tree
		    id="Tree" 
		    itemPress="onSelectedTree" 
		    items="{path: 'treeModel>/'}">
			<StandardTreeItem press="onPressNode" title="{treeModel>text}"/>	
		</Tree>			
<!--	Esta cosa se comento para poner el nuevo tree		
				<t:TreeTable
                    id="TreeTableBasic"
                    rows="{path:'treeModel>/TechnicalUbicationSet', 
	                    parameters : {
							countMode: 'Inline',
							treeAnnotationProperties : {
							    hierarchyNodeFor : 'Tplnr',
							    hierarchyLevelFor : 'HierarchyLevel',
							    hierarchyParentNodeFor : 'Idsup',
							    hierarchyDrillStateFor : 'DrillState'
						    }
				        }
                    }"
                    enableSelectAll="false"
                    ariaLabelledBy="title">-->
<!--                <toolbar>
                    <m:Toolbar>
                        <m:Title id="title" text="Clothing"/>
                        <m:ToolbarSpacer/>
                        <m:Button text="Collapse all" press="onCollapseAll"/>
                        <m:Button text="Expand first level" press="onExpandFirstLevel"/>
                    </m:Toolbar>
                </toolbar>-->
<!--                <t:columns>
                    <t:Column width="13rem">
                        <Label text="{i18n>ubication}"/>
                        <t:template>
                            <Text text="{Tplnr}"/>
                        </t:template>
                    </t:Column>
                </t:columns>
            </t:TreeTable>-->			
		</semantic:content>

	</semantic:MasterPage>

</mvc:View>